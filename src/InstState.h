#include "lutin.h"

#ifndef __INSTSTATE_H_
#define __INSTSTATE_H_

namespace lutinCompiler
{

	class State6: public State
	{
	public:
		 State6 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};

	class State10: public State
	{
	public:
		 State10 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};

	class State11: public State
	{
	public:
		 State11 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};

	class State12: public State
	{
	public:
		 State12 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};

	class State13: public State
	{
	public:
		 State13 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};

	class State14: public State
	{
	public:
		 State14 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
		class State15: public State
	{
	public:
		 State15 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
			class State16: public State
	{
	public:
		 State16 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
			class State17: public State
	{
	public:
		 State17 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
			class State18: public State
	{
	public:
		 State18 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
			class State19: public State
	{
	public:
		 State19 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
				class State31: public State
	{
	public:
		 State31 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
			class State32: public State
	{
	public:
		 State32 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
		class State33: public State
	{
	public:
		 State33 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
		class State34: public State
	{
	public:
		 State34 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
		class State35: public State
	{
	public:
		 State35 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State36: public State
	{
	public:
		 State36 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State37: public State
	{
	public:
		 State37 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State38: public State
	{
	public:
		 State38 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State39: public State
	{
	public:
		 State39 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State40: public State
	{
	public:
		 State40 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State41: public State
	{
	public:
		 State41 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State42: public State
	{
	public:
		 State42 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State43: public State
	{
	public:
		 State43 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State44: public State
	{
	public:
		 State44 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};

						class State45: public State
	{
	public:
		 State45 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State46: public State
	{
	public:
		 State46 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};
						class State47: public State
	{
	public:
		 State47 (const char* name);
		 void print();
		 bool transition(Automat & automat, Symbol * s);
		 void errorDiagnostic(Automat&);
	};






}

#endif // __INSTSTATE_H_